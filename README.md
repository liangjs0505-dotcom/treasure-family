# Treasure Family Frontend（宝藏之家前端）

超市进销存和结账。货物录入扫条码，结账按条码加入账单，确认时选择现金或刷卡。

本地运行：

```bash
npm install
npm run dev
```

开发地址：http://localhost:5173/

货物、结账和流水都请求后端 `http://localhost:8080`。开发改动放在 `feat_v1`。
